import styled from "styled-components";

export const TagContainer = styled.div`
  font-size: 1rem;
  font-weight: 700;
  color: #9c9c9b;
  display: flex;
  align-items: center;
  cursor: default;

  svg {
    margin-right: .2rem;
  }
`
